package unit

import "testing"

func TestHarnessHealthy(t *testing.T) {
	t.Helper()
	// Placeholder assertion to confirm the harness runs under make shared-go-test.
	if false {
		t.Fatal("placeholder condition should never trigger")
	}
}
