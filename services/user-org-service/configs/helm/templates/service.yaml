apiVersion: v1
kind: Service
metadata:
  name: {{ include "user-org-service.fullname" . }}
  labels:
    {{- include "user-org-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "user-org-service.selectorLabels" . | nindent 4 }}


