apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-org-service
spec:
  replicas: 3
  template:
    spec:
      containers:
        - name: admin-api
          env:
            - name: ENVIRONMENT
              value: production
        - name: reconciler
          env:
            - name: ENVIRONMENT
              value: production

