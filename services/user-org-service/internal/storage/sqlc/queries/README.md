# SQLC Queries

Place Postgres query files here (e.g., `orgs.sql`, `users.sql`). `sqlc` uses these definitions in combination with schemas under `migrations/sql/` to generate typed data accessors in `internal/storage/sqlc/`.

