apiVersion: v1
kind: Service
metadata:
  name: {{ include "api-router-service.fullname" . }}
  labels:
    {{- include "api-router-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "api-router-service.selectorLabels" . | nindent 4 }}

