# Speckit Alignment Review: 018-ui-update

**Date**: 2025-01-27  
**Reviewer**: AI Assistant  
**Purpose**: Review existing work in `018-ui-update` and align with speckit development workflow

---

## Executive Summary

The `018-ui-update` folder contains valuable work from another team, but it does **not** follow the speckit development workflow structure. The folder is missing the core `spec.md` file and the standard artifacts generated by `/speckit.plan` and `/speckit.tasks` commands.

**Status**: ⚠️ **Needs Alignment**

---

## Current State Analysis

### ✅ What Exists

| File | Status | Content Quality | Speckit Alignment |
|------|--------|-----------------|------------------|
| `tasks.md` | ✅ Present | High - Detailed task breakdown | ⚠️ Manual format, not from `/speckit.tasks` |
| `testing-plan.md` | ✅ Present | High - Comprehensive E2E strategy | ⚠️ Should be in `spec.md` or `plan.md` |
| `architecture-recommendations.md` | ✅ Present | High - Good technical guidance | ⚠️ Should be in `research.md` or `plan.md` |
| `screenshots/` | ✅ Present | N/A - Visual references | ✅ Acceptable as supporting material |

### ❌ What's Missing (Required by Speckit Workflow)

| File/Directory | Required By | Purpose | Status |
|----------------|-------------|---------|--------|
| `spec.md` | Core requirement | Feature specification with user stories, requirements, success criteria | ❌ **MISSING** |
| `plan.md` | `/speckit.plan` Phase 0-1 | Implementation plan with technical context, constitution check | ❌ **MISSING** |
| `research.md` | `/speckit.plan` Phase 0 | Research findings resolving "NEEDS CLARIFICATION" items | ❌ **MISSING** |
| `data-model.md` | `/speckit.plan` Phase 1 | Entity definitions, relationships, validation rules | ❌ **MISSING** |
| `quickstart.md` | `/speckit.plan` Phase 1 | Developer onboarding guide | ❌ **MISSING** |
| `contracts/` | `/speckit.plan` Phase 1 | API contracts (OpenAPI/GraphQL schemas) | ❌ **MISSING** |

---

## Speckit Workflow Requirements

### Expected Structure

According to `.specify/templates/plan-template.md` and `/speckit.plan` command, a feature spec should have:

```text
specs/018-ui-update/
├── spec.md              # ⚠️ MISSING - Core feature specification
├── plan.md              # ⚠️ MISSING - Generated by /speckit.plan
├── research.md          # ⚠️ MISSING - Phase 0 output
├── data-model.md        # ⚠️ MISSING - Phase 1 output
├── quickstart.md        # ⚠️ MISSING - Phase 1 output
├── contracts/           # ⚠️ MISSING - Phase 1 output (OpenAPI schemas)
├── tasks.md             # ⚠️ EXISTS but not from /speckit.tasks
└── screenshots/         # ✅ EXISTS - Supporting material
```

### Workflow Steps

1. **Create `spec.md`** - Feature specification following `.specify/templates/spec-template.md`
2. **Run `/speckit.plan`** - Generates:
   - `plan.md` (Technical Context, Constitution Check, Project Structure)
   - `research.md` (Phase 0: Resolves all "NEEDS CLARIFICATION")
   - `data-model.md` (Phase 1: Entities, relationships)
   - `quickstart.md` (Phase 1: Developer guide)
   - `contracts/` (Phase 1: API contracts)
3. **Run `/speckit.tasks`** - Generates `tasks.md` from spec + plan
4. **Run `/speckit.analyze`** - Validates coverage and consistency

---

## Content Mapping: Existing → Speckit Structure

### Mapping Strategy

| Existing File | Speckit Destination | Action Required |
|---------------|---------------------|----------------|
| `tasks.md` | `tasks.md` (regenerated) | Extract user stories → `spec.md`, then regenerate via `/speckit.tasks` |
| `testing-plan.md` | `spec.md` (Testing section) + `plan.md` (Testing strategy) | Extract testing requirements → `spec.md`, strategy → `plan.md` |
| `architecture-recommendations.md` | `research.md` (Phase 0) + `plan.md` (Technical Context) | Extract decisions → `research.md`, recommendations → `plan.md` |

### Detailed Mapping

#### From `tasks.md` → `spec.md`

**Extract:**
- **User Stories**: The 5 phases can be converted to prioritized user stories:
  - P1: Foundation (Centralized Config, HTTP Client)
  - P2: Design System (Theming, Layout Components)
  - P3: Page Implementation (Login, Dashboard, Admin Pages)
  - P4: Testing Infrastructure (Playwright tests)
  - P5: Cleanup & Documentation

**Extract Requirements:**
- FR-001: System MUST centralize API URL configuration in single module
- FR-002: System MUST standardize HTTP client usage (httpClient/publicClient)
- FR-003: System MUST implement system-aware theming (light/dark mode)
- FR-004: System MUST provide comprehensive Playwright E2E test coverage
- ... (extract from task descriptions)

#### From `testing-plan.md` → `spec.md` + `plan.md`

**To `spec.md` (Requirements section):**
- NFR-001: E2E tests MUST execute in < 2 minutes for smoke tests
- NFR-002: E2E tests MUST support remote dev cluster testing
- NFR-003: Tests MUST cover critical paths (login, API keys, members, usage)

**To `plan.md` (Technical Context):**
- Testing: Playwright with Chromium/Firefox/WebKit
- Test Environment: Remote dev cluster (`portal.172.232.58.222.nip.io`)
- Test Strategy: Smoke tests (fast) + Integration tests (comprehensive)

#### From `architecture-recommendations.md` → `research.md` + `plan.md`

**To `research.md` (Phase 0 decisions):**
- Decision: Centralize URL configuration in `/src/config/api.ts`
- Rationale: Eliminates duplication, single source of truth
- Alternatives: Keep distributed logic (rejected - maintenance burden)

- Decision: Use `publicClient` for unauthenticated requests
- Rationale: Consistent error handling, correlation IDs
- Alternatives: Native fetch() (rejected - inconsistent behavior)

- Decision: Extract health check to singleton pattern
- Rationale: Prevents React re-rendering issues
- Alternatives: React component with guards (rejected - still flickers)

**To `plan.md` (Technical Context):**
- Language/Version: TypeScript 5.x, React 18, Vite
- Primary Dependencies: TailwindCSS, shadcn/ui, Playwright
- Testing: Playwright E2E tests, Vitest unit tests
- Target Platform: Web browser (Chrome, Firefox, Safari)
- Performance Goals: Login page loads < 3s, no visible flickering

---

## Constitution Check Alignment

### Required Gates (from `.specify/memory/constitution.md`)

| Gate | Current Coverage | Status |
|------|-----------------|--------|
| **API-First** | ✅ UI is client-only, uses existing REST APIs | ✅ PASS |
| **Stateless** | ✅ Frontend is stateless, uses sessionStorage | ✅ PASS |
| **Security** | ⚠️ Mentions auth but needs explicit security section | ⚠️ NEEDS CLARIFICATION |
| **Observability** | ❌ No health/metrics endpoints mentioned | ❌ MISSING |
| **Testing** | ✅ Comprehensive Playwright plan | ✅ PASS |
| **Performance** | ✅ Performance goals defined | ✅ PASS |

### Missing Constitution Elements

1. **Security**: Need explicit mention of:
   - CSRF token handling
   - XSS prevention
   - Secure session storage
   - NetworkPolicies (K8s)

2. **Observability**: Need to specify:
   - Health endpoints (`/health`, `/ready`)
   - Metrics endpoints (if applicable)
   - Logging strategy (structured logs)

---

## Recommended Action Plan

### Phase 1: Create Core `spec.md` (Priority: P1)

**Steps:**
1. Extract user stories from `tasks.md` (5 phases → 5 prioritized stories)
2. Extract functional requirements from task descriptions
3. Extract non-functional requirements from `testing-plan.md`
4. Define success criteria (measurable metrics)
5. Add clarifications section (resolve any ambiguities)

**Template**: Use `.specify/templates/spec-template.md`

**Estimated Effort**: 2-3 hours

### Phase 2: Run `/speckit.plan` (Priority: P1)

**Steps:**
1. Ensure on feature branch: `feature/018-ui-update`
2. Run: `/speckit.plan` (no arguments needed if spec.md exists)
3. Review generated `plan.md`:
   - Fill Technical Context (extract from `architecture-recommendations.md`)
   - Verify Constitution Check gates
   - Review Project Structure
4. Review generated `research.md`:
   - Consolidate decisions from `architecture-recommendations.md`
   - Resolve all "NEEDS CLARIFICATION" items
5. Review generated `data-model.md`:
   - Add entities if applicable (User, APIKey, Organization, etc.)
6. Review generated `quickstart.md`:
   - Add setup instructions
   - Add testing instructions from `testing-plan.md`
7. Review generated `contracts/`:
   - Verify API contracts match existing backend APIs

**Estimated Effort**: 1-2 hours

### Phase 3: Regenerate `tasks.md` (Priority: P2)

**Steps:**
1. Run: `/speckit.tasks` (no arguments needed)
2. Compare generated `tasks.md` with existing one
3. Merge any unique tasks from existing file
4. Ensure task naming follows convention: `T-S018-P{phase}-{task}`

**Estimated Effort**: 30 minutes

### Phase 4: Run `/speckit.analyze` (Priority: P2)

**Steps:**
1. Run: `/speckit.analyze`
2. Address any findings:
   - Coverage gaps
   - Inconsistencies
   - Constitution violations
3. Re-run until report shows full coverage

**Estimated Effort**: 30 minutes

### Phase 5: Archive/Reorganize Existing Files (Priority: P3)

**Options:**
- **Option A**: Move to `archive/` subdirectory
- **Option B**: Extract useful content, then delete
- **Option C**: Keep as reference in `docs/` directory

**Recommendation**: Option B - Extract content into speckit structure, then archive originals

---

## Quick Start: Immediate Actions

### Step 1: Create `spec.md` Skeleton

```bash
cd /home/dev/ai-aas
cp .specify/templates/spec-template.md specs/018-ui-update/spec.md
```

Then fill in:
- Feature name: "UI Update & Admin Portal Rebuild"
- User stories (extract from `tasks.md` phases)
- Requirements (extract from task descriptions)
- Success criteria (from `testing-plan.md`)

### Step 2: Run Speckit Commands

```bash
# Ensure on feature branch
git checkout feature/018-ui-update

# Generate plan and artifacts
/speckit.plan

# Generate tasks
/speckit.tasks

# Validate
/speckit.analyze
```

### Step 3: Review and Refine

- Review generated files
- Merge unique content from existing files
- Ensure constitution compliance
- Update `llms.txt` with new spec links

---

## Key Differences: Current vs. Speckit

| Aspect | Current Approach | Speckit Approach |
|-------|------------------|------------------|
| **Specification** | Implicit in tasks | Explicit `spec.md` with user stories |
| **Planning** | Architecture recommendations | Structured `plan.md` with constitution check |
| **Research** | Embedded in recommendations | Dedicated `research.md` with decisions |
| **Tasks** | Manual task list | Generated from spec + plan via `/speckit.tasks` |
| **Contracts** | Not specified | OpenAPI schemas in `contracts/` |
| **Quickstart** | Not provided | Developer guide in `quickstart.md` |
| **Constitution** | Not explicitly checked | Explicit gate evaluation in `plan.md` |

---

## Conclusion

The existing work in `018-ui-update` is **high quality** but needs to be **restructured** to align with the speckit development workflow. The main gaps are:

1. ❌ Missing core `spec.md` file
2. ❌ Missing `plan.md` with constitution check
3. ❌ Missing Phase 0/1 artifacts (`research.md`, `data-model.md`, `quickstart.md`, `contracts/`)
4. ⚠️ `tasks.md` exists but not generated via `/speckit.tasks`

**Recommendation**: Follow the action plan above to bring the spec into full alignment with speckit workflow. The existing content is valuable and can be preserved by mapping it into the proper speckit structure.

---

## Next Steps

1. ✅ Review this document
2. ⏭️ Create `spec.md` (extract from existing files)
3. ⏭️ Run `/speckit.plan` to generate artifacts
4. ⏭️ Run `/speckit.tasks` to regenerate tasks
5. ⏭️ Run `/speckit.analyze` to validate
6. ⏭️ Archive or reorganize existing files

