# Knative Serving Components
# Note: Main Knative installation is via knative-config.yaml (infra/k8s/knative-serving)
# This file only manages the net-istio networking layer
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: knative-net-istio-development
  namespace: argocd
  labels:
    environment: development
    app: knative-serving
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: platform-development
  source:
    repoURL: https://github.com/otherjamesbrown/ai-aas
    targetRevision: develop  # Change to main after merge
    path: infra/k8s/knative/net-istio
  destination:
    server: https://kubernetes.default.svc
    namespace: knative-serving
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
