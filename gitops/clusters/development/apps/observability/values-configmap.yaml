apiVersion: v1
kind: ConfigMap
metadata:
  name: observability-values
  namespace: observability
  labels:
    app.kubernetes.io/managed-by: argocd
    environment: development
data:
  values.yaml: |
    global:
      environment: development

    prometheus:
      prometheusSpec:
        retention: "30d"
        scrapeInterval: "30s"

    alertmanager:
      config:
        global:
          resolve_timeout: 5m
        route:
          receiver: default
        receivers:
          - name: default
            slack_configs:
              - channel: "#platform-infra"
                send_resolved: true
                title: "[development] {{ .CommonAnnotations.summary }}"
                text: "{{ .CommonAnnotations.description }}"
