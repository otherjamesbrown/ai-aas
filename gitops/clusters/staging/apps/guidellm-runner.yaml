apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: guidellm-runner-staging
  namespace: argocd
  labels:
    environment: staging
    app: guidellm-runner
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: platform-staging
  source:
    repoURL: https://github.com/otherjamesbrown/guidellm-runner
    targetRevision: staging
    path: deployments/helm/guidellm-runner
    helm:
      valueFiles:
        - values-staging.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: staging
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
