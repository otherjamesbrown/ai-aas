apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: platform-staging
  namespace: argocd
spec:
  description: Platform infrastructure applications for staging
  destinations:
    - namespace: '*'
      server: https://kubernetes.default.svc
  sourceRepos:
    - '*'
  clusterResourceWhitelist:
    - group: '*'
      kind: '*'

