# Example environment overrides for build/test automation.

# Default service to target when running Make commands.
SERVICE=all

# Toggle metrics collection when running `make check`.
METRICS=false

# Default Git reference when triggering remote CI.
REF=current

# Optional notes to attach to `make ci-remote` runs.
NOTES=

# S3-compatible bucket used for metrics uploads.
METRICS_BUCKET=ai-aas-build-metrics

# Custom endpoint for MinIO (leave empty for AWS S3).
METRICS_ENDPOINT=

