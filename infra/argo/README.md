# ArgoCD Manifests

Contains ArgoCD `Application` and `ApplicationSet` manifests that reconcile infrastructure Helm charts. Keep manifests declarative and reference charts in `../helm/` with environment-specific overrides.
