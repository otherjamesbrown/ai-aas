# Secrets Bundles

This directory will contain templates and automation for syncing environment-specific secrets into the cluster (e.g., Linode Secret Manager bundles, Sealed Secrets manifests). Populate during the secrets bootstrap phase.
