# Environment Overview

Artifacts generated by this Terraform stack are written to the `.generated/`
directory. Regional deployment defaults to the value configured in
`default_region` (currently `fr-par`), but can be overridden via the
`region_overrides` map for individual environments.
