# Grafana Admin Secret
# NOTE: For production, replace this with a proper secret management solution
# (e.g., Sealed Secrets, External Secrets Operator, or Vault)
apiVersion: v1
kind: Secret
metadata:
  name: grafana-admin
  namespace: monitoring
  labels:
    app: grafana
type: Opaque
stringData:
  # TODO: Replace with a strong password in production
  # This is a placeholder - should be managed via external secret management
  admin-password: "ChangeMeInProduction123!"
