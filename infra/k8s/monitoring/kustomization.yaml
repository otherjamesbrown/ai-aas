apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: monitoring

resources:
  - otel-collector.yaml
  - cost-config.yaml

# Include subdirectory kustomizations
# Note: These are managed by separate ArgoCD applications
# - grafana/
# - loki/
# - promtail/
# - dashboards/

commonLabels:
  app.kubernetes.io/part-of: observability
