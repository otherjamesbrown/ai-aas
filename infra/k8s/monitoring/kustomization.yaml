apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: monitoring

# Base config resources moved to config/ subdirectory
# and managed by monitoring-config ArgoCD application

# Include subdirectory kustomizations
# Note: These are managed by separate ArgoCD applications
# - grafana/
# - loki/
# - promtail/
# - dashboards/

commonLabels:
  app.kubernetes.io/part-of: observability
