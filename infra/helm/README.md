# Helm Charts

This directory stores Helm charts and values overlays deployed to each environment via ArgoCD. Charts defined here should remain infrastructure-focused (ingress, observability, security bundles, sample service). Application services belong in their respective service repositories or directories.
