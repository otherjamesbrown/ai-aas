apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: platform-bootstrap
  namespace: secrets
  labels:
    environment: development
spec:
  encryptedData:

    grafana-admin: PLACEHOLDER-grafana-admin

    registry-token: PLACEHOLDER-registry-token

  template:
    metadata:
      labels:
        environment: development
      annotations:





    type: Opaque
